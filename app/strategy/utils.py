from services import StrategyLogicOperator

operator = StrategyLogicOperator()


def parse_strategy(strategy):
    operation = None
    # получили логический оператор по ходу парсинга:
    if operation == "EQUAL":
        
